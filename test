-- Animation Asset ID Printer

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- ============================================================
-- DEFAULT ROBLOX ANIMATION IDs TO FILTER OUT
-- ============================================================
local DEFAULT_ANIMATION_IDS = {
    ["http://www.roblox.com/asset/?id=180435571"] = true,
    ["rbxassetid://180435571"] = true,
    ["rbxassetid://507767714"] = true,
    ["rbxassetid://507767791"] = true,
    ["rbxassetid://507767792"] = true,
    ["rbxassetid://507765000"] = true,
    ["rbxassetid://507765644"] = true,
    ["rbxassetid://507766388"] = true,
    ["rbxassetid://507766666"] = true,
    ["rbxassetid://507767968"] = true,
    ["rbxassetid://507784897"] = true,
    ["rbxassetid://507770239"] = true,
    ["rbxassetid://507768375"] = true,
    ["rbxassetid://522635514"] = true,
    ["rbxassetid://913402848"] = true,
    ["rbxassetid://913376220"] = true,
    ["rbxassetid://616010382"] = true,
    ["rbxassetid://616006778"] = true,
    ["rbxassetid://616008087"] = true,
    ["rbxassetid://616013216"] = true,
    ["rbxassetid://616015781"] = true,
    ["rbxassetid://616016027"] = true,
    ["rbxassetid://616000974"] = true,
    ["rbxassetid://616007727"] = true,
    ["rbxassetid://2510235"]   = true,
    ["rbxassetid://2506281"]   = true,
    ["rbxassetid://2510238"]   = true,
    ["rbxassetid://2506268"]   = true,
    ["rbxassetid://182393478"] = true,
    ["rbxassetid://182494918"] = true,
    ["rbxassetid://182494909"] = true,
    ["rbxassetid://182494910"] = true,
}

local DEFAULT_NUMERIC_IDS = {
    [507767714]=true,[507767791]=true,[507767792]=true,
    [507765000]=true,[507765644]=true,[507766388]=true,
    [507766666]=true,[507767968]=true,[507784897]=true,
    [507770239]=true,[507768375]=true,[522635514]=true,
    [616010382]=true,[616006778]=true,[616008087]=true,
    [616013216]=true,[616015781]=true,[616016027]=true,
    [616000974]=true,[616007727]=true,[2510235]=true,
    [2506281]=true,  [2510238]=true,  [2506268]=true,
    [182393478]=true,[182494918]=true,[182494909]=true,
    [182494910]=true,[180435571]=true,
}

local function isDefaultAnimation(animId)
    if DEFAULT_ANIMATION_IDS[animId] then return true end
    local numId = tonumber(animId:match("%d+$"))
    if numId and DEFAULT_NUMERIC_IDS[numId] then return true end
    return false
end

local function extractNumericId(animId)
    return animId:match("%d+$") or animId
end

-- ============================================================
-- GUI CONSTRUCTION  — transparent grey/white style
-- ============================================================
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "AnimDetectorGUI"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.Parent = playerGui

-- Main frame
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 290, 0, 390)
mainFrame.Position = UDim2.new(0, 20, 0.5, -195)
mainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
mainFrame.BackgroundTransparency = 0.25
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Parent = screenGui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 10)
mainCorner.Parent = mainFrame

local mainStroke = Instance.new("UIStroke")
mainStroke.Color = Color3.fromRGB(255, 255, 255)
mainStroke.Transparency = 0.80
mainStroke.Thickness = 1
mainStroke.Parent = mainFrame

-- ── Title bar ───────────────────────────────────────────────
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 30)
titleBar.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
titleBar.BackgroundTransparency = 0.90
titleBar.BorderSizePixel = 0
titleBar.Parent = mainFrame

local titleCorner = Instance.new("UICorner")
titleCorner.CornerRadius = UDim.new(0, 10)
titleCorner.Parent = titleBar

-- Extend the bottom of the title bar so its lower corners don't show
local titleFix = Instance.new("Frame")
titleFix.Size = UDim2.new(1, 0, 0.5, 0)
titleFix.Position = UDim2.new(0, 0, 0.5, 0)
titleFix.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
titleFix.BackgroundTransparency = 0.90
titleFix.BorderSizePixel = 0
titleFix.Parent = titleBar

local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, -50, 1, 0)
titleLabel.Position = UDim2.new(0, 12, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "Anim Detector"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextTransparency = 0.15
titleLabel.TextSize = 11
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Parent = titleBar

-- Divider under title
local divider = Instance.new("Frame")
divider.Size = UDim2.new(1, -16, 0, 1)
divider.Position = UDim2.new(0, 8, 0, 30)
divider.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
divider.BackgroundTransparency = 0.85
divider.BorderSizePixel = 0
divider.Parent = mainFrame

-- ── Close button ─────────────────────────────────────────────
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 20, 0, 20)
closeBtn.Position = UDim2.new(1, -24, 0, 5)
closeBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.BackgroundTransparency = 0.85
closeBtn.BorderSizePixel = 0
closeBtn.Text = "✕"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.TextTransparency = 0.20
closeBtn.TextSize = 10
closeBtn.Font = Enum.Font.GothamBold
closeBtn.ZIndex = 5
closeBtn.Parent = mainFrame

local closeBtnCorner = Instance.new("UICorner")
closeBtnCorner.CornerRadius = UDim.new(1, 0)
closeBtnCorner.Parent = closeBtn

closeBtn.MouseEnter:Connect(function()
    closeBtn.BackgroundTransparency = 0.65
end)
closeBtn.MouseLeave:Connect(function()
    closeBtn.BackgroundTransparency = 0.85
end)
closeBtn.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)

-- ── Stats bar ────────────────────────────────────────────────
local statsBar = Instance.new("Frame")
statsBar.Size = UDim2.new(1, -24, 0, 28)
statsBar.Position = UDim2.new(0, 12, 0, 38)
statsBar.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
statsBar.BackgroundTransparency = 0.92
statsBar.BorderSizePixel = 0
statsBar.Parent = mainFrame

local statsCorner = Instance.new("UICorner")
statsCorner.CornerRadius = UDim.new(0, 6)
statsCorner.Parent = statsBar

local statsStroke = Instance.new("UIStroke")
statsStroke.Color = Color3.fromRGB(255, 255, 255)
statsStroke.Transparency = 0.82
statsStroke.Thickness = 1
statsStroke.Parent = statsBar

local countLabel = Instance.new("TextLabel")
countLabel.Size = UDim2.new(0.55, 0, 1, 0)
countLabel.Position = UDim2.new(0, 10, 0, 0)
countLabel.BackgroundTransparency = 1
countLabel.Text = "Captured: 0"
countLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
countLabel.TextSize = 11
countLabel.Font = Enum.Font.Gotham
countLabel.TextXAlignment = Enum.TextXAlignment.Left
countLabel.Parent = statsBar

-- Clear button
local clearBtn = Instance.new("TextButton")
clearBtn.Size = UDim2.new(0, 68, 0, 20)
clearBtn.Position = UDim2.new(1, -74, 0.5, -10)
clearBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
clearBtn.BackgroundTransparency = 0.88
clearBtn.BorderSizePixel = 0
clearBtn.Text = "Clear All"
clearBtn.TextColor3 = Color3.fromRGB(220, 220, 220)
clearBtn.TextSize = 10
clearBtn.Font = Enum.Font.GothamBold
clearBtn.Parent = statsBar

local clearCorner = Instance.new("UICorner")
clearCorner.CornerRadius = UDim.new(0, 5)
clearCorner.Parent = clearBtn

local clearStroke = Instance.new("UIStroke")
clearStroke.Color = Color3.fromRGB(255, 255, 255)
clearStroke.Transparency = 0.82
clearStroke.Thickness = 1
clearStroke.Parent = clearBtn

clearBtn.MouseEnter:Connect(function()
    clearBtn.BackgroundTransparency = 0.75
end)
clearBtn.MouseLeave:Connect(function()
    clearBtn.BackgroundTransparency = 0.88
end)

-- ── Scroll container ─────────────────────────────────────────
local scrollContainer = Instance.new("Frame")
scrollContainer.Size = UDim2.new(1, -24, 0, 282)
scrollContainer.Position = UDim2.new(0, 12, 0, 74)
scrollContainer.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
scrollContainer.BackgroundTransparency = 0.95
scrollContainer.BorderSizePixel = 0
scrollContainer.ClipsDescendants = true
scrollContainer.Parent = mainFrame

local scrollContainerCorner = Instance.new("UICorner")
scrollContainerCorner.CornerRadius = UDim.new(0, 8)
scrollContainerCorner.Parent = scrollContainer

local scrollContainerStroke = Instance.new("UIStroke")
scrollContainerStroke.Color = Color3.fromRGB(255, 255, 255)
scrollContainerStroke.Transparency = 0.87
scrollContainerStroke.Thickness = 1
scrollContainerStroke.Parent = scrollContainer

local scrollFrame = Instance.new("ScrollingFrame")
scrollFrame.Size = UDim2.new(1, 0, 1, 0)
scrollFrame.BackgroundTransparency = 1
scrollFrame.BorderSizePixel = 0
scrollFrame.ScrollBarThickness = 3
scrollFrame.ScrollBarImageColor3 = Color3.fromRGB(200, 200, 200)
scrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
scrollFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
scrollFrame.Parent = scrollContainer

local listLayout = Instance.new("UIListLayout")
listLayout.Padding = UDim.new(0, 5)
listLayout.Parent = scrollFrame

local listPadding = Instance.new("UIPadding")
listPadding.PaddingTop = UDim.new(0, 6)
listPadding.PaddingLeft = UDim.new(0, 6)
listPadding.PaddingRight = UDim.new(0, 6)
listPadding.Parent = scrollFrame

-- Empty state label
local emptyLabel = Instance.new("TextLabel")
emptyLabel.Size = UDim2.new(1, 0, 1, 0)
emptyLabel.BackgroundTransparency = 1
emptyLabel.Text = "No emotes detected yet.\nPlay an animation to see it here."
emptyLabel.TextColor3 = Color3.fromRGB(140, 140, 140)
emptyLabel.TextTransparency = 0.30
emptyLabel.TextSize = 12
emptyLabel.Font = Enum.Font.Gotham
emptyLabel.TextWrapped = true
emptyLabel.Parent = scrollContainer

-- ── Status bar ────────────────────────────────────────────────
local statusBar = Instance.new("Frame")
statusBar.Size = UDim2.new(1, -24, 0, 26)
statusBar.Position = UDim2.new(0, 12, 1, -34)
statusBar.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
statusBar.BackgroundTransparency = 0.92
statusBar.BorderSizePixel = 0
statusBar.Parent = mainFrame

local statusCorner = Instance.new("UICorner")
statusCorner.CornerRadius = UDim.new(0, 6)
statusCorner.Parent = statusBar

local statusBarStroke = Instance.new("UIStroke")
statusBarStroke.Color = Color3.fromRGB(255, 255, 255)
statusBarStroke.Transparency = 0.82
statusBarStroke.Thickness = 1
statusBarStroke.Parent = statusBar

local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(1, -10, 1, 0)
statusLabel.Position = UDim2.new(0, 10, 0, 0)
statusLabel.BackgroundTransparency = 1
statusLabel.Text = "● Listening"
statusLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
statusLabel.TextSize = 11
statusLabel.Font = Enum.Font.GothamBold
statusLabel.TextXAlignment = Enum.TextXAlignment.Left
statusLabel.Parent = statusBar

-- ============================================================
-- ANIMATION ENTRY LOGIC
-- ============================================================
local capturedCount = 0
local seenIds = {}

local function flashStatus(message, color)
    statusLabel.Text = message
    statusLabel.TextColor3 = color or Color3.fromRGB(200, 200, 200)
    task.delay(2, function()
        statusLabel.Text = "● Listening"
        statusLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
    end)
end

local function addAnimationEntry(animId)
    local numericId = extractNumericId(animId)

    if seenIds[numericId] then
        flashStatus("Already captured: " .. numericId)
        return
    end

    seenIds[numericId] = true
    capturedCount = capturedCount + 1
    emptyLabel.Visible = false
    countLabel.Text = "Captured: " .. capturedCount

    -- Row frame
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, 0, 0, 50)
    row.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    row.BackgroundTransparency = 0.92
    row.BorderSizePixel = 0
    row.Parent = scrollFrame

    local rowCorner = Instance.new("UICorner")
    rowCorner.CornerRadius = UDim.new(0, 7)
    rowCorner.Parent = row

    local rowStroke = Instance.new("UIStroke")
    rowStroke.Color = Color3.fromRGB(255, 255, 255)
    rowStroke.Transparency = 0.85
    rowStroke.Thickness = 1
    rowStroke.Parent = row

    -- Number badge
    local badge = Instance.new("Frame")
    badge.Size = UDim2.new(0, 26, 0, 26)
    badge.Position = UDim2.new(0, 9, 0.5, -13)
    badge.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    badge.BackgroundTransparency = 0.85
    badge.BorderSizePixel = 0
    badge.Parent = row

    local badgeCorner = Instance.new("UICorner")
    badgeCorner.CornerRadius = UDim.new(0, 5)
    badgeCorner.Parent = badge

    local badgeNum = Instance.new("TextLabel")
    badgeNum.Size = UDim2.new(1, 0, 1, 0)
    badgeNum.BackgroundTransparency = 1
    badgeNum.Text = tostring(capturedCount)
    badgeNum.TextColor3 = Color3.fromRGB(255, 255, 255)
    badgeNum.TextTransparency = 0.15
    badgeNum.TextSize = 11
    badgeNum.Font = Enum.Font.GothamBold
    badgeNum.Parent = badge

    -- ID label
    local idLabel = Instance.new("TextLabel")
    idLabel.Size = UDim2.new(1, -115, 0, 18)
    idLabel.Position = UDim2.new(0, 44, 0, 7)
    idLabel.BackgroundTransparency = 1
    idLabel.Text = numericId
    idLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    idLabel.TextTransparency = 0.10
    idLabel.TextSize = 12
    idLabel.Font = Enum.Font.GothamBold
    idLabel.TextXAlignment = Enum.TextXAlignment.Left
    idLabel.TextTruncate = Enum.TextTruncate.AtEnd
    idLabel.Parent = row

    -- Full rbxassetid sub-label
    local fullLabel = Instance.new("TextLabel")
    fullLabel.Size = UDim2.new(1, -115, 0, 14)
    fullLabel.Position = UDim2.new(0, 44, 0, 28)
    fullLabel.BackgroundTransparency = 1
    fullLabel.Text = "rbxassetid://" .. numericId
    fullLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
    fullLabel.TextTransparency = 0.20
    fullLabel.TextSize = 9
    fullLabel.Font = Enum.Font.Gotham
    fullLabel.TextXAlignment = Enum.TextXAlignment.Left
    fullLabel.TextTruncate = Enum.TextTruncate.AtEnd
    fullLabel.Parent = row

    -- Copy button
    local copyBtn = Instance.new("TextButton")
    copyBtn.Size = UDim2.new(0, 54, 0, 30)
    copyBtn.Position = UDim2.new(1, -62, 0.5, -15)
    copyBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    copyBtn.BackgroundTransparency = 0.82
    copyBtn.BorderSizePixel = 0
    copyBtn.Text = "Copy"
    copyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    copyBtn.TextTransparency = 0.10
    copyBtn.TextSize = 10
    copyBtn.Font = Enum.Font.GothamBold
    copyBtn.Parent = row

    local copyCorner = Instance.new("UICorner")
    copyCorner.CornerRadius = UDim.new(0, 6)
    copyCorner.Parent = copyBtn

    local copyStroke = Instance.new("UIStroke")
    copyStroke.Color = Color3.fromRGB(255, 255, 255)
    copyStroke.Transparency = 0.78
    copyStroke.Thickness = 1
    copyStroke.Parent = copyBtn

    copyBtn.MouseButton1Click:Connect(function()
        setclipboard(numericId)
        copyBtn.Text = "✓"
        copyBtn.BackgroundTransparency = 0.70
        flashStatus("Copied: " .. numericId)
        task.delay(1.5, function()
            copyBtn.Text = "Copy"
            copyBtn.BackgroundTransparency = 0.82
        end)
    end)

    copyBtn.MouseEnter:Connect(function()
        if copyBtn.Text == "Copy" then
            copyBtn.BackgroundTransparency = 0.70
        end
    end)
    copyBtn.MouseLeave:Connect(function()
        if copyBtn.Text == "Copy" then
            copyBtn.BackgroundTransparency = 0.82
        end
    end)

    -- Slide-in fade
    row.BackgroundTransparency = 1
    TweenService:Create(row, TweenInfo.new(0.25, Enum.EasingStyle.Quad), {
        BackgroundTransparency = 0.92
    }):Play()

    task.defer(function()
        scrollFrame.CanvasPosition = Vector2.new(0, math.huge)
    end)
end

-- Clear all entries
clearBtn.MouseButton1Click:Connect(function()
    for _, child in ipairs(scrollFrame:GetChildren()) do
        if child:IsA("Frame") then child:Destroy() end
    end
    seenIds = {}
    capturedCount = 0
    countLabel.Text = "Captured: 0"
    emptyLabel.Visible = true
    flashStatus("Cleared all entries")
end)

-- ============================================================
-- ANIMATION DETECTION
-- ============================================================
local function onAnimationPlayed(track)
    if track.Animation then
        local animId = track.Animation.AnimationId
        if animId and animId ~= "" and not isDefaultAnimation(animId) then
            addAnimationEntry(animId)
        end
    end
end

local character = player.Character or player.CharacterAdded:Wait()

local function connectHumanoid(char)
    local humanoid = char:WaitForChild("Humanoid")
    humanoid.AnimationPlayed:Connect(onAnimationPlayed)
end

connectHumanoid(character)

player.CharacterAdded:Connect(function(newChar)
    connectHumanoid(newChar)
end)

print("[AnimDetector] GUI loaded. Press ✕ to close.")
