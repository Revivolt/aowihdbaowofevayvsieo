-- // G

local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/deeeity/mercury-lib/master/src.lua"))()
local gui = Library:create{
    Theme = Library.Themes.Serika
}
local tab = gui:tab{
    Icon = "rbxassetid://6034996695",
    Name = "Aimbot"
}
tab:button({
    Name = "show prompt",
    Description = "Very important button",
    Callback = function()
        print("Button clicked")
    end,
})

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

local toggleGui = Instance.new("ScreenGui")
toggleGui.Name = "ToggleGui"
toggleGui.ResetOnSpawn = false
toggleGui.ZIndexBehavior = Enum.ZIndexBehavior.Global
toggleGui.Parent = (RunService:IsStudio() and LocalPlayer.PlayerGui) or game:GetService("CoreGui")

local toggleButton = Instance.new("TextButton")
toggleButton.Name = "ToggleButton"
toggleButton.Size = UDim2.fromOffset(90, 30)
toggleButton.Position = UDim2.fromOffset(10, 10)
toggleButton.BackgroundColor3 = Color3.fromRGB(50, 52, 55)
toggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleButton.Font = Enum.Font.SourceSansBold
toggleButton.TextSize = 14
toggleButton.Text = "Hide UI"
toggleButton.BorderSizePixel = 0
toggleButton.Parent = toggleGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 6)
corner.Parent = toggleButton

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(226, 183, 20)
stroke.Thickness = 1
stroke.Parent = toggleButton

-- Dragging logic
local dragging = false
local wasDragged = false
local dragOffset = Vector2.new()
local dragThreshold = 5

toggleButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        wasDragged = false
        -- Store click offset within the button so cursor stays anchored to that point
        dragOffset = Vector2.new(
            input.Position.X - toggleButton.AbsolutePosition.X,
            input.Position.Y - toggleButton.AbsolutePosition.Y
        )
    end
end)

toggleButton.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
        -- Fire toggle only if the button wasn't meaningfully dragged
        if not wasDragged then
            Library.Toggled = not Library.Toggled
            Library:show(Library.Toggled)
            toggleButton.Text = Library.Toggled and "Hide UI" or "Show UI"
        end
    end
end)

RunService.RenderStepped:Connect(function()
    if dragging then
        local mousePos = UserInputService:GetMouseLocation()
        local newPos = Vector2.new(mousePos.X - dragOffset.X, mousePos.Y - dragOffset.Y)
        -- Only mark as dragged if moved beyond threshold
        if not wasDragged then
            local currentPos = Vector2.new(toggleButton.AbsolutePosition.X, toggleButton.AbsolutePosition.Y)
            if (newPos - currentPos).Magnitude > dragThreshold then
                wasDragged = true
            end
        end
        toggleButton.Position = UDim2.fromOffset(newPos.X, newPos.Y)
    end
end)
