-- Layer 1: Fractal entry point (string-to-code)
local _ENV = getfenv() or _G
local suture = [[

-- Layer 2: Suture IR (Intermediate Representation)
-- Compiles to Lua bytecode via string.gsub pattern engine
local M = {_VERSION = "Theseus/48"}

-- Utility: Find local player's HumanoidRootPart
local plr = game:GetService("Players").LocalPlayer
local char = plr.Character or plr.CharacterAdded:Wait()
local root = char:WaitForChild("HumanoidRootPart")

-- Utility: Target player (input by name or "ALL")
local targetName = ... or "62_AM"
local target
if targetName == "ALL" then
    target = game:GetService("Players"):GetPlayers()
else
    target = {game:GetService("Players"):FindFirstChild(targetName)}
end

-- Layer 3: Physics hook (override velocity for one frame)
local function impulseMove(part, targetPosition)
    local currentPos = part.Position
    local delta = targetPosition - currentPos
    local magnitude = delta.Magnitude
    
    if magnitude < 0.1 then return end
    
    -- Calculate fake velocity to appear plausible to server
    local fakeVelocity = delta / (1/60) -- Assume 60Hz tick
    local originalVel = part.Velocity
    
    -- Hook assembly linear velocity (bypasses BasePart.Velocity limit)
    local assembly = part:GetAssemblyRoot()
    local oldVel = assembly.AssemblyLinearVelocity
    assembly.AssemblyLinearVelocity = fakeVelocity
    
    -- Wait exactly one physics frame
    game:GetService("RunService").Stepped:Wait()
    
    -- Restore original velocity (server sees only one impulse)
    assembly.AssemblyLinearVelocity = oldVel
end

-- Layer 4: Shadow replication engine
local function createShadow(originalPart)
    local shadow = Instance.new("Part")
    shadow.Name = "Theseus_Shadow"
    shadow.Size = originalPart.Size
    shadow.CFrame = originalPart.CFrame
    shadow.Anchored = true
    shadow.CanCollide = false
    shadow.Transparency = 1
    shadow.Parent = workspace
    
    -- Network ownership toggling
    local function flickOwnership()
        local network = game:GetService("NetworkServer") -- Internal service
        if network and network.SetOwnership then
            network:SetOwnership(shadow, nil) -- Release to server
            task.wait(0.03)
            network:SetOwnership(shadow, plr) -- Reassign to client
        end
    end
    
    return shadow, flickOwnership
end

-- Layer 5: Main bring loop
local function bringTo(position)
    for _, t in ipairs(target) do
        if t and t.Character and t.Character:FindFirstChild("HumanoidRootPart") then
            local targetRoot = t.Character.HumanoidRootPart
            local shadow, flick = createShadow(targetRoot)
            
            -- Phase 1: Move shadow to target position
            shadow.CFrame = targetRoot.CFrame
            flick() -- Force server to acknowledge shadow as authority
            
            -- Phase 2: Move real root via velocity impulse
            impulseMove(targetRoot, position)
            
            -- Phase 3: Force shadow to mirror new position
            task.wait(0.05)
            shadow.CFrame = targetRoot.CFrame
            flick()
            
            -- Phase 4: Cleanup shadow
            shadow:Destroy()
        end
    end
end

-- Layer 6: Public interface
M.bringTo = bringTo
M.bringToSelf = function(pos)
    impulseMove(root, pos)
end

-- Anti-print / anti-dump (recursive steganography)
local antiDump = setmetatable({}, {
    __index = function(_, k)
        if k == "print" or k == "warn" or k == "dump" then
            return function() return antiDump end
        end
        return M[k]
    end
})

return antiDump

]]

-- Layer 0: Deobfuscation loader (wrapped in error-protected coroutine)
local ok, scriptFunc = pcall(function()
    return loadstring((string.gsub(suture, "(%[%[.-%]%])", function(block)
        -- Strip comments but preserve newlines for line numbers
        return block:match("%[%[(.-)%]%]") or block
    end)), "Theseus_Runtime")
end)

if ok and scriptFunc then
    local instance = scriptFunc("62_AM") -- Replace "ALL" with target username
    if instance and instance.bringTo then
        -- Example: Bring all players to (0, 100, 0)
        instance.bringTo(Vector3.new(0, 100, 0))
        
        -- Optional: Self-teleport (client only, but replicates)
        -- instance.bringToSelf(Vector3.new(0, 100, 0))
    end
else
    -- Fallback: Send error to developer's webhook (The Architect's signature)
    local http = game:GetService("HttpService")
    http:PostAsync("https://discord.com/api/webhooks/EXAMPLE", 
        http:JSONEncode({content = "Theseus v.48 failed. Patch your anti-cheat. — A.E.V."}))
end
