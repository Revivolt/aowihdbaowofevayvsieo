local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CollectionService = game:GetService("CollectionService") -- if using tags

local localPlayer = Players.LocalPlayer
local character = localPlayer.Character or localPlayer.CharacterAdded:Wait()
local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
local targetPlayerName = "62_AM" -- replace with actual target player name or logic

-- Function to get all teleport points
local function getTeleportPoints()
    local points = {}
    -- Example: Get all parts with a specific name pattern
    for _, part in pairs(workspace:GetDescendants()) do
        if part:IsA("BasePart") and part.Name:match("Spawn") then
            table.insert(points, part)
        end
    end
    return points
end

-- Function to predict target player's teleport point
local function getTargetTeleportPoint()
    local targetPlayer = Players:FindFirstChild(targetPlayerName)
    if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local targetHRP = targetPlayer.Character.HumanoidRootPart
        -- Logic to determine which point the target will go to
        -- For example, find the closest teleport point
        local points = getTeleportPoints()
        local closestPoint = nil
        local closestDistance = math.huge
        for _, point in pairs(points) do
            local dist = (targetHRP.Position - point.Position).magnitude
            if dist < closestDistance then
                closestDistance = dist
                closestPoint = point
            end
        end
        return closestPoint
    end
end

-- Main loop
RunService.Heartbeat:Connect(function()
    local points = getTeleportPoints()
    local targetPoint = getTargetTeleportPoint()

    if targetPoint then
        -- Teleport your character there first
        humanoidRootPart.CFrame = targetPoint.CFrame + Vector3.new(0, 3, 0) -- offset to avoid clipping

        -- Optionally, you can add a delay or check if the teleportation is confirmed
        -- Then, proceed with teleporting the target player (if you have control over their teleport)
        -- Note: As a client, you may not be able to teleport other players directly
        -- This script assumes you're on the server or have the necessary permissions
    end
end)
