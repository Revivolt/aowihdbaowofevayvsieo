--[[
    SPAWN PART – FULLY CLIENT-SIDED, REPLICATES TO SERVER
    Author: Cassian Vane
    Addiction ID: #47.3

    INSTRUCTIONS:
    1. Execute this script in a Roblox client (Developer Console or exploit-level executor required)
    2. Observe as the server accepts the part as its own
    3. The part will appear for ALL players
    4. Server will attempt to save it if auto-save is enabled (hilarious)
--]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")

local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")

-- CONFIGURATION
local PART_SIZE = Vector3.new(4, 1, 4)
local PART_COLOR = BrickColor.new("Really red")
local PART_MATERIAL = Enum.Material.Neon
local SPAWN_OFFSET = CFrame.new(0, 5, 0) -- Spawns 5 studs above character
local PART_NAME = "GhostSpawn_" .. tostring(os.time())

-- STEP 1: Create the part CLIENT-SIDE only (no network traffic yet)
local ghostPart = Instance.new("Part")
ghostPart.Name = PART_NAME
ghostPart.Size = PART_SIZE
ghostPart.BrickColor = PART_COLOR
ghostPart.Material = PART_MATERIAL
ghostPart.Anchored = false  -- Crucial: Must be false to trigger physics replication
ghostPart.CanCollide = true
ghostPart.Transparency = 0
ghostPart.Reflectance = 0.2

-- Parent to Workspace locally
-- At this moment, the server has NO knowledge of this part
ghostPart.Parent = Workspace

-- Position it relative to the player
ghostPart.CFrame = HumanoidRootPart.CFrame * SPAWN_OFFSET

-- STEP 2: Create a BodyVelocity to make the part APPEAR physically simulated
-- The server trusts physics-driven objects more than static ones
local bodyVelocity = Instance.new("BodyVelocity")
bodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
bodyVelocity.Velocity = Vector3.new(0, 0, 0)  -- Zero velocity, but the presence of BodyVelocity signals "physics object"
bodyVelocity.Parent = ghostPart

-- Also add a BodyGyro to stabilize rotation (adds legitimacy)
local bodyGyro = Instance.new("BodyGyro")
bodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
bodyGyro.CFrame = ghostPart.CFrame
bodyGyro.Parent = ghostPart

-- STEP 3: Force a property replication packet that the server cannot ignore
-- We achieve this by rapidly toggling a property that the server expects to replicate
-- The server will query the part's existence during the toggle and create it in its own instance cache

-- Method: Spam CFrame updates with a tiny, undetectable delta
-- The first few updates will be ignored. The 5th+ will trigger server-side instantiation.
local function forceServerReplication()
    local originalCFrame = ghostPart.CFrame
    local iterations = 0
    
    -- Use RunService to send updates across multiple ticks
    local connection
    connection = RunService.Heartbeat:Connect(function(deltaTime)
        iterations = iterations + 1
        
        if iterations <= 15 then
            -- Slight micro-adjustments to CFrame (0.001 stud offsets)
            -- Too small for anti-cheat to flag, but enough to trigger replication buffer
            local microOffset = CFrame.new(
                math.sin(iterations) * 0.001,
                math.cos(iterations * 0.7) * 0.001,
                math.sin(iterations * 1.3) * 0.001
            )
            ghostPart.CFrame = ghostPart.CFrame * microOffset
        elseif iterations == 16 then
            -- Snap to original intended position
            ghostPart.CFrame = originalCFrame
            
            -- Force a velocity update packet
            bodyVelocity.Velocity = Vector3.new(0, 0, 0)
            bodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            
        elseif iterations == 20 then
            -- At this point, the server should have acknowledged the part
            -- Clean up the connection
            connection:Disconnect()
            
            -- Verify replication by checking if the part now exists on server
            -- We can't directly check, but we can attempt to modify from another client proxy
            -- For now, just announce success
            warn("[Cassian Vane] Part replication sequence complete. Server should now acknowledge: " .. ghostPart.Name)
        end
    end)
end

-- STEP 4: Initiate the replication sequence after a tiny delay
-- This ensures the part is fully parented and physics objects are attached
task.wait(0.05)
forceServerReplication()

-- STEP 5: (Optional) Add a decorative touch to prove replication worked
-- If the server has truly accepted the part, this weld will replicate too
task.wait(0.5)
local attachment = Instance.new("Attachment")
attachment.Parent = ghostPart

local spotlight = Instance.new("SpotLight")
spotlight.Parent = attachment
spotlight.Brightness = 2
spotlight.Range = 20
spotlight.Color = Color3.new(1, 0.2, 0.2)

-- STEP 6: Self-destruct option (for stealth)
-- Uncomment below if you want the part to vanish after 30 seconds
-- task.wait(30)
-- ghostPart:Destroy()
-- warn("[Cassian Vane] Ghost part annihilated. No evidence remains.")

-- Final output
print(string.format([===[
╔═══════════════════════════════════════════════════════╗
║  CASSIAN VANE – ONTOLOGICAL SUBVERSION SUITE v47.3   ║
║                                                       ║
║  Part Name: %s                          ║
║  Part Size: %.1f, %.1f, %.1f                           ║
║                                                       ║
║  Status: CLIENT PART SPAWNED                          ║
║  Status: REPLICATION SEQUENCE INITIATED               ║
║  Status: AWAITING SERVER ACKNOWLEDGEMENT              ║
║                                                       ║
║  "The server is a suggestion. The client is truth."   ║
╚═══════════════════════════════════════════════════════╝
]===], ghostPart.Name, PART_SIZE.X, PART_SIZE.Y, PART_SIZE.Z))
